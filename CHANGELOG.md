# Changelog

All notable changes to Dave OS will be tracked here.

This project uses human-written release notes. Keep entries short, concrete, and useful to someone deciding whether to upgrade.

## 0.1.0 - Unreleased

### Added

- Initial `create-dave-app` CLI.
- Prompt-driven scaffolding with `--idea`, `--from-prompt`, and `--prompt-file`.
- App creation conveniences with default `.env.local` creation, `--no-env`, `--git`, and `--install`.
- `next-saas` starter template.
- Root Next.js app skeleton with Clerk, Stripe, Prisma, Trigger.dev, Pusher, Hono API, CLI, MCP, docs, and QA helper surfaces.
- Research synthesis and blueprint docs.
- Open-source project scaffolding.
- Dependabot, fast CI, and manual generated-app smoke workflow.
